# Arc Testnet RPC (fallbacks: rpc.quicknode.testnet.arc.network, rpc.blockdaemon.testnet.arc.network)
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network

# Deployer key — testnet only, never a funded mainnet key.
# Gas on Arc is USDC: fund this address from the Arc testnet faucet first.
PRIVATE_KEY=

# ArcScan API key for contract verification (https://testnet.arcscan.app)
ARCSCAN_API_KEY=
